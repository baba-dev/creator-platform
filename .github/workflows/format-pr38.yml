name: Format PR 38 hardening

on:
  push:
    branches:
      - feat/job-reconciliation-resolution
    paths:
      - ".github/workflows/format-pr38.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/job-reconciliation-resolution
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
        with:
          version: 11.19.0
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Format hardened reconciliation files
        run: |
          pnpm exec prettier --write             apps/web/src/components/admin/job-resolution-actions.tsx             docs/image-generation.md             packages/generation/src/reconciliation.ts             packages/generation/test/reconciliation.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add             apps/web/src/components/admin/job-resolution-actions.tsx             docs/image-generation.md             packages/generation/src/reconciliation.ts             packages/generation/test/reconciliation.test.ts
          git commit -m "style: format held-credit reconciliation hardening"
          git push origin HEAD:feat/job-reconciliation-resolution
